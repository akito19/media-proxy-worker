[package]
name = "media-proxy-worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.7"
serde = { version = "1.0", features = ["derive"] }
console_error_panic_hook = "0.1"

[profile.release]
opt-level = "s"
lto = true
